
import React from 'react';
import { Card, CardContent } from '@/components/ui/card';
import { Info } from 'lucide-react';
import { motion } from 'framer-motion';

const InfoCard = () => {
  return null; // Completely remove the component
};

export default InfoCard;
